const label = {
    MOTION: 'Motion'
};
const button = {
    SIGN_IN: 'SIGN IN'
};

const place_holder = {
    EMAIL_ADDRESS: "Email address",
    PASSWORD: "Password"
}

module.exports = {
    label: label,
    button: button,
    place_holder: place_holder
}